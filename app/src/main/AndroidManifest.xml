<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dechproduct.rebooapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReBooApplication">
        <activity android:name="activity.SettingActivity"></activity>
        <activity android:name="activity.EffectCatalogActivity" />
        <activity android:name="activity.PresetAudioActivity" />
        <activity android:name="activity.OpenAudioActivity" />
        <activity android:name="activity.HomeActivity" />
        <activity android:name="activity.SplashScreenActivity" />
        <activity android:name="activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>